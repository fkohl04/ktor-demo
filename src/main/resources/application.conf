ktor {
    development = true
    deployment {
        port = 8081
        port = ${?PORT}
    }
    application {
        modules = [ com.example.ApplicationKt.module ]
    }
}

datasource {
    url = ${?DATASOURCE_URL}
    user = ${?DATASOURCE_USER}
    password = ${?DATASOURCE_PASSWORD}
    driver = ${?DATASSOURCE_DRIVER}
}
